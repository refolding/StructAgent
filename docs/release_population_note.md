# Release population note

This private archive has now been expanded beyond the initial skeleton.

Added material:

- full-system implementation guide;
- Annika/Maria collaboration protocol;
- skills-only usage instructions;
- sanitized A2A sender template;
- structural-biology execution skills from Annika's workspace;
- Maria paper-reading/database/review/distill/discovery skills.

Sanitization performed before commit:

- removed local absolute user paths from copied skill notes;
- excluded identity files, memory, chat logs, routing config and API credentials;
- searched for obvious private tokens, host/user IDs and private filesystem paths;
- kept only generic placeholder environment variables in setup templates.

Remaining limitations:

- the public archive still needs a final maintainer review before being made public;
- the reviewer-only evidence bundle is not included here;
- Supplementary Table 3 remains a paper-submission blocker until exact E3-4 to E3-7 recovery entries are completed.
